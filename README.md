# 🚀 FastAPI Blog Backend

Это легковесный и безопасный backend-сервис для блога, построенный на FastAPI. Проект включает в себя полноценную систему аутентификации, разделение ролей (User/Admin), автоматическую очистку HTML от вредоносного кода и полную контейнеризацию.

Проект построен на принципе разделения ответственности:

- Client (Swagger/Frontend): Отправляет HTTP-запрос.

- FastAPI (Main App): Принимает запрос и направляет его в соответствующий роутер в app/api/.

- Dependencies (get_db, auth): Перед выполнением логики извлекает сессию БД и проверяет JWT-токен пользователя.

- CRUD Layer: Выполняет конкретные операции с базой данных (SELECT, INSERT).

- SQLAlchemy (Models): Описывает структуру таблиц в SQLite.

- Pydantic (Schemas): Проверяет входящие данные и форматирует исходящий JSON-ответ.

## 🛠 Технологический стек

- **Framework:** FastAPI + Uvicorn
- **Database:** SQLite (через aiosqlite)
- **ORM:** SQLAlchemy 2.0 (Async)
- **Migrations:** Alembic
- **Validation:** Pydantic v2
- **Security:** JWT (Access & Refresh tokens), Passlib (bcrypt)
- **Sanitization:** Bleach (защита от XSS)
- **Containerization:** Docker & Docker Compose
- **Testing:** Pytest + HTTPX

## ✨ Основные возможности

### 🔐 Аутентификация и роли

- **JWT Auth:** Использование Access и Refresh токенов для безопасных сессий
- **Ролевая модель:**
  - **USER:** Может просматривать посты и категории
  - **ADMIN:** Имеет полный доступ к CRUD операциям и управлению пользователями
- **Защита паролей:** Хеширование с использованием bcrypt

### 🛡 Безопасность

- **HTML Sanitization:** Весь входящий HTML-контент постов проходит через bleach. Разрешены только безопасные теги (p, b, a и др.), любые `<script>` или `onclick` атрибуты вырезаются
- **CORS:** Настроен белый список разрешенных доменов

### 📝 Управление контентом

- Публичное API для чтения постов и фильтрации по категориям
- Админ-панель (API) для создания, редактирования и удаления данных

## 🏗 Структура проекта

.
├── alembic/            # Миграции базы данных
├── app/
│   ├── api/            # Эндпоинты (auth, posts, categories, admin)
│   ├── crud/           # Логика работы с БД (Create, Read, Update, Delete)
│   ├── models/         # SQLAlchemy модели
│   ├── schemas/        # Pydantic схемы (валидация)
│   ├── security/       # JWT и хеширование паролей
│   └── utils/          # Вспомогательные функции (bleach)
├── tests/              # Автоматические тесты (Pytest)
├── docker-compose.yml  # Описание сервисов Docker
└── Dockerfile          # Инструкции по сборке образа
```

## 🚀 Быстрый запуск

### 1. Подготовка

Клонируйте репозиторий и создайте файл `.env` в корне проекта на основе примера:

```ini
DEBUG=True
DATABASE_URL="sqlite+aiosqlite:///./data/blog.db"
SECRET_KEY=your_secret_key_here
```

### 2. Запуск через Docker

Проект полностью автоматизирован. При запуске Docker Compose:

- Поднимутся контейнеры
- Автоматически применятся миграции Alembic
- Автоматически создастся начальный Администратор (Seeding)

Выполните команду:

```bash
docker-compose up --build
```

### 3. Доступ к API

- **API URL:** http://localhost:8000
- **Документация (Swagger):** http://localhost:8000/docs

## 🔑 Начальные данные (Admin)

После первого запуска вы можете войти в систему, используя данные по умолчанию (создаются автоматически скриптом `initial_data.py`):

- **Email:** admin@blog.com
- **Password:** admin

## 🧪 Тестирование

Для запуска тестов внутри контейнера используйте:

```bash
docker-compose exec app python -m pytest -v
```

Тесты проверяют регистрацию, логику JWT, права доступа админа и корректность работы очистки HTML.

## 📝 API Endpoints

### Публичные

- `GET /api/v1/posts` — Список всех постов
- `GET /api/v1/posts/{slug}` — Детальный просмотр поста
- `GET /api/v1/categories` — Список категорий

### Аутентификация

- `POST /api/v1/auth/register` — Регистрация
- `POST /api/v1/auth/login` — Получение токенов
- `POST /api/v1/auth/refresh` — Обновление access токена
- `GET /api/v1/auth/users/me` — Информация о текущем пользователе

### Администрирование

- `POST /api/v1/admin/posts` — Создание поста (Bleach защищен)
- `POST /api/v1/admin/categories` — Создание категории
- `PATCH /api/v1/admin/users/{id}/role` — Управление ролями (USER/ADMIN)
```
